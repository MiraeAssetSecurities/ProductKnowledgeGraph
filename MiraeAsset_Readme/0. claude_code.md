# Claude 사용 방법


### 1. VSCode Extention 설치

## 2. Claude Code 설치

```bash
curl -fsSL https://claude.ai/install.sh | bash
```

```bash
echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc
echo 'export CLAUDE_CODE_USE_BEDROCK=1'>> ~/.bashrc
echo 'export AWS_REGION=ap-northeast-2'>> ~/.bashrc
source ~/.bashrc
```
### 3. VSCode ssh remote 재연결

## 4. AWS CLI 설치
```bash
sudo snap install aws-cli --classic
```

## 5. AWS Config 등록
```bash
aws configure
```

```bash
ubuntu@ip-172-31-24-167:~/ProductKnowledgeGraph$ aws configure
AWS Access Key ID [****************JWEG]: 
AWS Secret Access Key [****************4Owq]: 
Default region name [ap-northeast-2]: 
Default output format [None]: 
```

끝